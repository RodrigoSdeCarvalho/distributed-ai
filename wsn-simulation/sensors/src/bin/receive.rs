use tungstenite::{connect, Message};
use url::Url;

fn main() {
    // let (mut socket, response) = connect(
    //     Url::parse("ws://127.0.0.1:8000/ws").unwrap()
    // ).expect("Can't connect");
    //
    // loop {
    //     let msg = socket.read_message().expect("Error reading message");
    //     println!("Received: {}", msg);
    // }
}
